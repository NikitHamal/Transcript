apply plugin: 'com.android.library'

android {
    namespace 'com.transcript.lib.vosk'
    compileSdkVersion 34
    defaultConfig {
        minSdkVersion 24
        targetSdkVersion 33
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    // Using stub classes instead of actual Vosk dependency
    // implementation 'org.vosk:vosk-android:0.3.38'
}
